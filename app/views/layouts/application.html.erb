<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @title %></title>
    <%= stylesheet_link_tag "application" %>
</head>
<body>
    <div id="whole_page">
        <div id="header">RailSpace</div>
        <div id="nav">
            <% #My own function of link_to_unless_current as defined in application_helper.rb %>
            <%= nav_link "Home", "site" %> | 
            <%= nav_link "About Us", "site", "about" %> |
            <%= nav_link "Help", "site", "help" %> |
            <%= nav_link "Register", "user", "register" %>
        </div>
        <div id="content">
            <% if flash[:notice] -%>
                <div id="notice"><%= flash[:notice].try(:html_safe) %></div>
            <% end -%>
            <%= yield %>
        </div>
        <% if ENV["RAILS_ENV"] == "development" %>
          <%= debug(params) %>
        <% end %>
    </div>
</body>
</html>